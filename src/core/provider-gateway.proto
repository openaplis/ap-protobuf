syntax = "proto3";

package gateway;

import "domain/client.proto";
import "search-request.proto";

service ProviderGateway {
  rpc getClient (SearchRequest) returns (ClientReply) {};
}

message ClientReply {
  domain.Client client = 1;
}
